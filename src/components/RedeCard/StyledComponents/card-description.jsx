import styled from 'styled-components';

const CardDescription = styled.p`
    font-size: 1em;
    max-width: 100%;
    width: auto;
    display:block;
    flex-direction:row;
    max-height:80px;
    overflow:hidden;
`;

export default CardDescription;
